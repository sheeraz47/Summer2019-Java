package day39_DecimalFormatterClass;

public class notes {

	/*
	 instance variable: created outside methods, within the class. (object)
     it's also called fields
    local variables: created within the methods. not visible to other
     methods can only be used within the method
    instance methods: methods that deos not have static specifier. but 
    do have return type. (object)
this. : used for calling instance variables, we used when local and instance variables have the same names

	 */
}
